const express = require('express');
const routes = express.Router();
const controllers = require('../controllers/');

//example Controller
// routes.get('/getAccount', controllers.AC);

module.exports = routes;
